<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.nine.crm.mapper.DepartmentMapper">
    <insert id="insert" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        INSERT INTO t_department(sn, name)
        VALUES (#{sn}, #{name})
    </insert>
    <update id="update">
        UPDATE t_department
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="sn != null">
                sn = #{sn},
            </if>
        </set>
        WHERE id = #{id}
    </update>
    <delete id="delete">
        DELETE
        FROM t_department
        WHERE id = #{id}
    </delete>
    <delete id="batchDelete">
        DELETE FROM t_department WHERE id IN
        <foreach collection="list" open="(" close=")" item="id" separator=",">
            #{id}
        </foreach>
    </delete>
    <select id="selectAll" resultType="cn.nine.crm.domain.Department">
        SELECT id, name, sn
        FROM t_department
    </select>
    <select id="selectById" resultType="cn.nine.crm.domain.Department"></select>
    <sql id="base_where">
        <where>
            <if test="keyword != null">
                and name like CONCAT("%",#{keyword},"%")
            </if>
        </where>
    </sql>
    <!--分页查询-->
    <select id="selectByCount" resultType="long">
        SELECT count(id) FROM t_department
        <include refid="base_where"/>
    </select>
    <select id="selectByQuery" resultType="cn.nine.crm.domain.Department">
        SELECT id, name, sn FROM t_department
        <include refid="base_where"/>
        limit #{start},#{pageSize}
    </select>
</mapper>